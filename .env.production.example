# Set this during `next build`; NEXT_PUBLIC_* values are embedded in the client bundle.
# Use /api only when your production reverse proxy forwards that path to the backend.
NEXT_PUBLIC_API_URL=https://api.example.com/v1
